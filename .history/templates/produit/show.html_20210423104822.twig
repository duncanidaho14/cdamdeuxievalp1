{% extends "base.html.twig" %}

{% block title %}
    produit {{produit.id }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            {{produit.titre}}
            {{produit.prix|number_format(2, '.', ',')}}
            <button><a href="{{path('produit_edit')}}"></a></button>
            <button type="submit">X</button>
        </div>
    </div>
{% endblock %}